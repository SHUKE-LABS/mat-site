---
import BaseLayout from '../layouts/BaseLayout.astro';
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';
import { plans, LEMONSQUEEZY_CHECKOUT } from '../config/pricing';

// The layout appends ` | My AI Team` to any page title; the home page wants the
// bare site title, so it passes none.
const description =
  'My AI Team is a tmux-based framework for running structured AI-agent sessions against a GitHub repo. Agents claim ready tickets, implement, open PRs, and merge them themselves.';

const frictions = [
  {
    heading: 'No babysitting terminals',
    body: 'Every session runs async. You drive it remotely over a two-way Telegram relay instead of watching a pane.',
  },
  {
    heading: 'No manual handoff',
    body: 'Agents pass work to each other through GitHub issues and labels, not copy-paste. Claiming a ticket swaps its ready label for assigned_to:<lock>.',
  },
  {
    heading: 'No single-agent bottleneck',
    body: 'Investigation, implementation, and review split across session modes and parallel backend slots.',
  },
];

const producing = [
  { name: 'explore', body: 'Investigate a question and crystallise a ticket. One agent, read-only.' },
  { name: 'caucus', body: 'A proposer and a challenger deliberate one topic down to tickets.' },
  { name: 'audit', body: 'A self-arming cadence auditor reviews merged work and files triage tickets.' },
];

const delivering = [
  { name: 'live', body: 'Hands-on work in the current directory: direct operations, or the quick patch workflow.' },
  { name: 'adhoc', body: 'One agent runs the full cycle: claim, implement, PR, merge, notify.' },
  { name: 'duo', body: 'Two-agent relay — a dev plans and implements, an independent reviewer gates and merges.' },
  { name: 'team', body: 'Three-agent relay — planner to dev to reviewer, for stricter role isolation.' },
];

// Buyer-facing view of the loop: the six stages a ticket passes through. The
// plan is reviewed before the build, so the two review stages are distinct.
const stages = [
  { name: 'Ticket', note: 'A goal enters the queue — a rough idea, a bug, a feature.' },
  { name: 'Plan', note: 'An agent drafts the approach, naming the real files and steps.' },
  { name: 'Plan review', note: 'An independent reviewer approves the approach before any code is written.' },
  { name: 'Build', note: 'An agent implements the change on its own branch and opens a PR.' },
  { name: 'Code review', note: 'A different agent reads the diff cold and signs off — or sends it back.' },
  { name: 'Merge', note: 'The change lands, and the queue re-arms for the next ticket.' },
];

// The trust checkpoints that keep the loop honest. Wording ported from
// shukelabs.com's My AI Team how-it-works page before it redirects here.
const guardrails = [
  {
    heading: 'A plan is approved first',
    body: 'An independent reviewer signs off on the approach before a line of code is written, so the build starts from an agreed design — not a guess.',
  },
  {
    heading: 'Review is independent',
    body: 'The agent that reviews the diff is not the one that wrote it, and it re-reads the change with fresh context — so review stays honest instead of rubber-stamping.',
  },
  {
    heading: 'Tests gate the merge',
    body: 'The suite runs on every change. Green merges; red sends the work back to the builder. Nothing lands on a failing test.',
  },
  {
    heading: 'You decide the direction',
    body: 'The team resolves what it can on its own and comes to you only for real decisions — product calls, scope, tradeoffs. When agents disagree on design, two of them deliberate to a conclusion instead of escalating.',
  },
];
---

<BaseLayout description={description}>
  <Nav />
  <main class="flex-1">
    <!-- Hero -->
    <section class="mx-auto max-w-5xl px-6 pt-14 pb-16">
      <p class="text-sm font-semibold uppercase tracking-wider text-muted">My AI Team</p>
      <h1 class="mt-3 text-4xl sm:text-5xl font-bold tracking-tight text-ink">
        Ship real code while you sleep.
      </h1>
      <p class="mt-5 max-w-2xl text-xl sm:text-2xl font-medium text-accent">
        My AI Team runs structured AI-agent sessions against your GitHub repo — agents claim ready
        tickets, implement on a branch, open PRs, and merge them themselves.
      </p>
      <p class="mt-4 max-w-2xl text-lg text-muted leading-relaxed">
        A tmux-based framework for solo developers and small teams. You set direction and step in
        on decisions and blockers; the routine review-and-merge loop is the agents' job.
      </p>
      <div class="mt-8 flex flex-wrap gap-3">
        <a
          href={LEMONSQUEEZY_CHECKOUT}
          class="inline-flex items-center rounded-lg bg-accent px-5 py-2.5 text-sm font-medium text-white transition-colors hover:bg-accent-hover"
          >Buy My AI Team</a
        >
        <a
          href="https://mat-docs.shukelabs.com"
          class="inline-flex items-center rounded-lg border border-line px-5 py-2.5 text-sm font-medium text-ink transition-colors hover:bg-surface-soft"
          >Read the docs</a
        >
      </div>
    </section>

    <!-- What it fixes -->
    <section class="border-y border-line bg-surface-soft">
      <div class="mx-auto max-w-5xl px-6 py-14">
        <h2 class="text-2xl font-bold text-ink">What it fixes</h2>
        <p class="mt-3 max-w-2xl text-muted">
          Three friction points surface once you use coding agents regularly. My AI Team is built around
          removing them.
        </p>
        <div class="mt-8 grid gap-5 sm:grid-cols-3">
          {
            frictions.map((item) => (
              <div class="rounded-2xl border border-line bg-surface p-6">
                <h3 class="font-semibold text-ink">{item.heading}</h3>
                <p class="mt-2 text-sm leading-relaxed text-muted">{item.body}</p>
              </div>
            ))
          }
        </div>
      </div>
    </section>

    <!-- Plans -->
    <section id="plans" class="mx-auto max-w-5xl px-6 py-14">
      <h2 class="text-2xl font-bold text-ink">Choose your plan</h2>
      <p class="mt-3 max-w-2xl text-muted">
        Every plan is annual access to My AI Team, licensed per device. Payments are handled
        securely by LemonSqueezy; the checkout page confirms the plan before purchase.
      </p>
      <div class="mt-8 grid gap-5 sm:grid-cols-3">
        {
          plans.map((plan) => (
            <div class="flex flex-col rounded-2xl border border-line bg-surface p-6">
              <span class="text-sm font-semibold uppercase tracking-wider text-accent">{plan.label}</span>
              <p class="mt-3">
                <span class="text-4xl font-bold text-ink">{plan.price}</span>
                <span class="ml-1 text-muted">{plan.period}</span>
              </p>
              <p class="mt-3 text-sm font-medium text-ink">Up to {plan.deviceLimit} devices</p>
              <p class="mt-2 text-sm text-muted">Annual access to My AI Team.</p>
              <a
                href={plan.checkoutUrl}
                class="mt-6 inline-flex items-center justify-center rounded-lg bg-accent px-5 py-2.5 text-sm font-medium text-white transition-colors hover:bg-accent-hover"
              >
                Choose {plan.label}
              </a>
            </div>
          ))
        }
      </div>
      <p class="mt-6 max-w-2xl text-sm text-muted leading-relaxed">
        Payments are handled by <strong class="font-medium text-ink">LemonSqueezy</strong>, our
        merchant of record. Your purchase is subject to LemonSqueezy's buyer terms and refund
        policy, shown at checkout, and to our
        <a
          href="https://shukelabs.com/terms#terms-of-sale"
          class="text-accent underline underline-offset-2 hover:text-accent-hover">terms of sale</a
        >
        and
        <a
          href="https://shukelabs.com/terms#refund-policy"
          class="text-accent underline underline-offset-2 hover:text-accent-hover">refund policy</a
        >.
      </p>
    </section>

    <!-- How it works -->
    <section id="how-it-works" class="border-y border-line bg-surface-soft">
      <div class="mx-auto max-w-5xl px-6 py-14">
        <h2 class="text-2xl font-bold text-ink">How it works</h2>
        <p class="mt-3 max-w-2xl text-muted">
          You kick off with an explore session; from there the loop runs itself — produce tickets,
          deliver them on branches, review and merge, and file what comes next back into the
          queue.
        </p>
        <img
          src="/images/my-ai-team-loop.svg"
          alt="Autonomous AI loop: a human kicks off with the Explorer agent; Planner, Reviewer, Developer and Auditor cycle tickets from plan to review to merge, then file new tickets that feed back to the Planner."
          width="740"
          height="605"
          class="mt-6 mx-auto block h-auto w-full"
        />

        <!-- Six-stage concept row: two review stages, not one -->
        <div class="mt-10">
          <h3 class="text-sm font-semibold uppercase tracking-wider text-muted">The autonomous loop</h3>
          <div class="mt-6 flex flex-col sm:flex-row sm:flex-wrap sm:items-stretch sm:gap-y-3">
            {
              stages.map((stage, i) => (
                <>
                  {i > 0 && (
                    <div
                      class="flex items-center justify-center py-2 text-2xl text-muted sm:px-3 sm:py-0"
                      aria-hidden="true"
                    >
                      <span class="sm:hidden">↓</span>
                      <span class="hidden sm:inline">→</span>
                    </div>
                  )}
                  <div class="rounded-2xl border border-line bg-surface p-6 sm:min-w-[10rem] sm:flex-1">
                    <span class="text-lg font-semibold text-ink">{stage.name}</span>
                    <p class="mt-2 text-sm text-muted leading-relaxed">{stage.note}</p>
                  </div>
                </>
              ))
            }
          </div>
          <div
            class="mt-6 flex items-center gap-3 rounded-xl border border-dashed border-line bg-surface p-5"
          >
            <span class="text-2xl text-accent" aria-hidden="true">↻</span>
            <p class="text-sm text-muted leading-relaxed">
              Each merge re-arms the cycle. The team finishes one ticket and picks up the next
              without waiting to be re-invoked.
            </p>
          </div>
        </div>

        <!-- Trust checkpoints -->
        <div class="mt-12">
          <h3 class="text-sm font-semibold uppercase tracking-wider text-muted">
            Where the guardrails are
          </h3>
          <div class="mt-6 grid gap-4 sm:grid-cols-2">
            {
              guardrails.map((item) => (
                <div class="rounded-xl border border-line bg-surface p-5">
                  <h4 class="text-base font-semibold text-ink">{item.heading}</h4>
                  <p class="mt-2 text-sm text-muted leading-relaxed">{item.body}</p>
                </div>
              ))
            }
          </div>
        </div>

        <p class="mt-8 text-center text-sm">
          <a href="https://mat-docs.shukelabs.com" class="text-accent hover:underline"
            >Read the documentation for the detailed session modes &rarr;</a
          >
        </p>
      </div>
    </section>

    <!-- Session modes -->
    <section id="modes" class="mx-auto max-w-5xl px-6 py-14">
      <h2 class="text-2xl font-bold text-ink">Session modes</h2>
      <p class="mt-3 max-w-2xl text-muted">
        Work splits along one seam: producing tickets versus delivering them. GitHub issues and
        labels are the membrane between the two sides.
      </p>
      <div class="mt-8 grid gap-8 sm:grid-cols-2">
        <div>
          <h3 class="text-sm font-semibold uppercase tracking-wider text-muted">
            Produce tickets — never delivery code
          </h3>
          <dl class="mt-4 space-y-4">
            {
              producing.map((mode) => (
                <div class="rounded-xl border border-line p-5">
                  <dt class="font-mono text-sm font-semibold text-accent">{mode.name}</dt>
                  <dd class="mt-1.5 text-sm leading-relaxed text-muted">{mode.body}</dd>
                </div>
              ))
            }
          </dl>
        </div>
        <div>
          <h3 class="text-sm font-semibold uppercase tracking-wider text-muted">
            Deliver — branch, PR, merge
          </h3>
          <dl class="mt-4 space-y-4">
            {
              delivering.map((mode) => (
                <div class="rounded-xl border border-line p-5">
                  <dt class="font-mono text-sm font-semibold text-accent">{mode.name}</dt>
                  <dd class="mt-1.5 text-sm leading-relaxed text-muted">{mode.body}</dd>
                </div>
              ))
            }
          </dl>
        </div>
      </div>
    </section>

    <!-- Get started -->
    <section id="get-started" class="border-y border-line bg-surface-soft">
      <div class="mx-auto max-w-5xl px-6 py-14">
        <h2 class="text-2xl font-bold text-ink">Get started</h2>
        <p class="mt-3 max-w-2xl text-muted">
          Buying a plan gets you the release tarball and your license key, both delivered through
          the LemonSqueezy checkout. Download the tarball, then from the directory it landed in:
        </p>
        <pre class="term mt-6"><code>tar -xzf mat.tar.gz
cd my-ai-team
./install.sh
mat activate &lt;LICENSE_KEY&gt;</code></pre>
        <p class="mt-4 max-w-2xl text-sm leading-relaxed text-muted">
          <code>install.sh</code> installs the runtime into
          <code>~/.local/share/my-ai-team</code>, links the commands into
          <code>~/.local/bin</code>, and registers the resident Telegram relay for your platform.
          <code>mat</code> runs right away without activation, but only an activated machine can
          <code>mat upgrade</code> later — so activate now.
        </p>
        <p class="mt-4 max-w-2xl text-sm leading-relaxed text-muted">
          The platform quickstarts in the
          <a href="https://mat-docs.shukelabs.com" class="text-accent hover:underline">docs</a>
          cover prerequisites, Telegram setup, your first session, and upgrades.
        </p>
      </div>
    </section>

    <!-- Next steps -->
    <section class="mx-auto max-w-5xl px-6 py-14">
      <h2 class="text-2xl font-bold text-ink">Next steps</h2>
      <div class="mt-6 grid gap-5 sm:grid-cols-3">
        <a
          href="https://mat-docs.shukelabs.com"
          class="rounded-2xl border border-line p-6 transition-colors hover:bg-surface-soft"
        >
          <h3 class="font-semibold text-ink">Documentation</h3>
          <p class="mt-2 text-sm text-muted">Quickstarts, session protocols, FAQ.</p>
        </a>
        <a
          href="/#plans"
          class="rounded-2xl border border-line p-6 transition-colors hover:bg-surface-soft"
        >
          <h3 class="font-semibold text-ink">Pricing &amp; license</h3>
          <p class="mt-2 text-sm text-muted">Plans, device limits, and the buy button.</p>
        </a>
        <a
          href="https://shukelabs.com"
          class="rounded-2xl border border-line p-6 transition-colors hover:bg-surface-soft"
        >
          <h3 class="font-semibold text-ink">SHUKE Labs</h3>
          <p class="mt-2 text-sm text-muted">The rest of what we build, and the blog.</p>
        </a>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>
